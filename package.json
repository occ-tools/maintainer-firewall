{
  "name": "maintainer-firewall",
  "version": "0.7.1",
  "description": "A maintainer-first GitHub Action that filters noisy issues and pull requests by evidence, reproducibility, scope, and project-specific rules.",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "action.yml",
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "SECURITY.md",
    "SUPPORT.md",
    "docs",
    "examples",
    "schema"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "ci": "npm run check && npm run evaluate && npm run verify:dist",
    "demo": "npm run build && node scripts/demo.mjs",
    "evaluate": "npm run build && node scripts/evaluate.mjs",
    "evaluate:ai": "npm run build && node scripts/evaluate-ai.mjs",
    "metrics:summary": "node scripts/summarize-metrics.mjs",
    "coverage": "vitest run --coverage",
    "market:check": "npm run ci && npm run coverage && npm run demo && npm audit --audit-level=moderate && npm pack --dry-run && node scripts/market-check.mjs",
    "release:check": "npm run check && npm run coverage && npm run demo && npm run evaluate && npm run verify:dist && npm audit --audit-level=moderate",
    "verify:dist": "npm run bundle && git diff --exit-code -- dist",
    "test": "vitest run",
    "check": "npm run build && npm run test"
  },
  "keywords": [
    "github-action",
    "open-source",
    "maintainers",
    "issue-triage",
    "pull-request-review"
  ],
  "author": "Hephaestus-DevKit",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Hephaestus-DevKit/maintainer-firewall.git"
  },
  "bugs": {
    "url": "https://github.com/Hephaestus-DevKit/maintainer-firewall/issues"
  },
  "homepage": "https://github.com/Hephaestus-DevKit/maintainer-firewall#readme",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "minimatch": "^10.0.1",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@vercel/ncc": "^0.44.1",
    "@vitest/coverage-v8": "^4.1.9",
    "fast-check": "^4.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
